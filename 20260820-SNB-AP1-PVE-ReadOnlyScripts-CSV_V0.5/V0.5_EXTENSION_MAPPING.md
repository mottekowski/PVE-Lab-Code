# V0.5 Extension Mapping

## Neue Collector und XLSX-Zuordnung

| Script | Scope | CSV-Kategorie | Was | Ziel-Reiter |
|---|---|---|---|---|
| `13-pve-permissions.sh` | clusterweit einmal | `ProxMoxCluster` | `PVEPermissions` | `ProxMox_Cluster` |
| `14-pve-ha-core.sh` | clusterweit einmal | `ProxMoxCluster` | `PVEHACore` | `ProxMox_Cluster` |
| `15-pve-ha-node.sh` | jeder ONLINE PVE-Node | `ProxMoxCluster` | `PVEHANode` | `ProxMox_Cluster` |

Die Template-Struktur bleibt unveraendert:

```text
Node/Scope | Kategorie | Parameter | Wert | Befehl/Quelle | Zeitstempel | Validiert (Y/N) | Kommentar
```

## Merge

Der bestehende Merge sucht generisch nach:

```text
<Run-ID>_*_*_ProxMoxCluster_*.csv
```

Daher werden 13/14/15 ohne Aenderung von `90-ap1-merge.sh` automatisch in:

```text
<Run-ID>_ProxMoxCluster_AllHosts.csv
```

aufgenommen.

## Bestehende Bereiche

- Auth/LDAP/User: weiterhin Script 06, unveraendert
- HA Basis: weiterhin Script 03, unveraendert; 14/15 ergaenzen die CORE-Sicht
- Network: Script 05, unveraendert
- Backup-Jobs/PBS-Abhaengigkeit: Script 06, unveraendert
- Ceph: Scripts 07-12 wie bisher
- SDN: keine neue CSV / kein neuer Collector
